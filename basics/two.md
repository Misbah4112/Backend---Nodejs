okay so basically we have a computer/mobile with a browser andour goal is to make a server that listens to the request of a browser. for this purpose, we make use of express which is a framework that listens to the request through /:home route e.g
/login: login details


npm init : means initializing an application with the help of node package manager